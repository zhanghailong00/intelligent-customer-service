# 嵌入式实训箱智能客服系统 — 项目设计文档

## 一、项目背景

### 公司业务
公司制作高校实训道具（嵌入式实验箱），主要客户为高校实验室。

### 业务痛点
- 技术支持团队反复回答相同的故障排查问题
- 高校老师使用设备时遇到问题，无法 24 小时获得帮助
- 培训资料分散，查找不方便

### 项目目标
基于多智能体架构，构建一个智能客服系统，覆盖（产品咨询【待定】）、故障排查（售后）、培训资料三大场景，支持多轮对话和人工介入审核。

---

## 二、业务场景分析

### 2.1 售前 — 产品咨询（待定）

### 2.2 售中 — 培训如何做实验

### 2.3 售后 — 使用问题
- 设备故障排查
- 软件问题
- 技术支持

---

## 三、技术架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────────────────────┐
│                       展示层                                 │
│  ┌─────────────────────────────────────────────────────┐   │
│  │  Gradio Web 界面                                    │   │
│  │  • 用户对话界面（web/app.py）                        │   │
│  │  • 知识库管理后台（web/admin.py）                    │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                       接口层                                 │
│                  FastAPI RESTful API                         │
├─────────────────────────────────────────────────────────────┤
│                     业务逻辑层                               │
│  ┌─────────────────────────────────────────────────────┐   │
│  │              LangGraph 状态图引擎                     │   │
│  │                                                      │   │
│  │  START → classifier → router → agent → hitl → END   │   │
│  │            ↑              ↓         ↓               │   │
│  │            └──────────────┘         │               │   │
│  │                    ↑                ↓               │   │
│  │                    └──── interrupt ─┘               │   │
│  │                                                      │   │
│  └─────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                   Agent 集群                         │   │
│  │  ┌───────────┬───────────────┬───────────────┐     │   │
│  │  │ 产品专家  │   技术支持    │    培训顾问    │     │   │
│  │  │  Agent   │    Agent     │     Agent     │     │   │
│  │  └───────────┴───────────────┴───────────────┘     │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                      数据访问层                              │
│  ┌─────────────────────────────────────────────────────┐   │
│  │                 RAG 检索引擎                          │   │
│  │  ┌──────────┬───────────┬───────────┐              │   │
│  │  │ 查询重写 │ 向量检索  │ 结果融合   │              │   │
│  │  └──────────┴───────────┴───────────┘              │   │
│  └─────────────────────────────────────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                       数据层                                │
│  ┌──────────────┬──────────────┬──────────────────────┐   │
│  │   ChromaDB   │    SQLite    │     文件系统          │   │
│  │  （向量存储）  │ （结构化数据） │   （知识库文档）     │   │
│  └──────────────┴──────────────┴──────────────────────┘   │
├─────────────────────────────────────────────────────────────┤
│                      基础设施层                              │
│  ┌──────────────┬──────────────┬──────────────────────┐   │
│  │   DeepSeek   │    豆包      │     通义千问          │   │
│  │   (主模型)   │  (备用模型)  │    (Embedding)       │   │
│  └──────────────┴──────────────┴──────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 数据处理流程

```
┌─────────────────────────────────────────────────────────────┐
│                      数据处理流程                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  原始资料（PDF/Word/图片）                                   │
│       ↓                                                     │
│  ┌─────────────────┐                                       │
│  │  MinerU API     │  ← PDF 转 Markdown                    │
│  │  (文档转换)     │                                       │
│  └────────┬────────┘                                       │
│           │                                                 │
│           ↓                                                 │
│  ┌─────────────────┐                                       │
│  │  文档切分       │  ← 按标题/段落切分                     │
│  │  (LangChain)    │                                       │
│  └────────┬────────┘                                       │
│           │                                                 │
│           ↓                                                 │
│  ┌─────────────────┐                                       │
│  │  向量化         │  ← 通义千问 Embedding API              │
│  │  (Embedding)    │                                       │
│  └────────┬────────┘                                       │
│           │                                                 │
│           ↓                                                 │
│  ┌─────────────────┐                                       │
│  │  存储           │  ← ChromaDB 向量数据库                 │
│  │  (ChromaDB)     │                                       │
│  └────────┬────────┘                                       │
│           │                                                 │
│           ↓                                                 │
│  用户查询时 → 返回文字 + 图片                                │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.3 LLM 降级策略

```
┌─────────────────────────────────────────────────────────────┐
│                      LLM 降级策略                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│  用户提问                                                    │
│       ↓                                                     │
│  ┌─────────────────┐                                       │
│  │  调用 DeepSeek  │  ← 主模型                             │
│  │  (主模型)       │                                       │
│  └────────┬────────┘                                       │
│           │                                                 │
│      ┌────┴────┐                                           │
│      ↓         ↓                                           │
│   成功       失败                                           │
│      │         │                                           │
│      ↓         ↓                                           │
│   返回结果   ┌─────────────────┐                           │
│              │  调用豆包       │  ← 备用模型               │
│              │  (备用模型)     │                           │
│              └────────┬────────┘                           │
│                       │                                     │
│                  ┌────┴────┐                               │
│                  ↓         ↓                               │
│               成功       失败                               │
│                  │         │                               │
│                  ↓         ↓                               │
│               返回结果   返回错误提示                        │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**降级策略说明**：

| 场景 | 处理方式 |
|------|----------|
| **DeepSeek 调用成功** | 直接返回结果 |
| **DeepSeek 调用失败** | 自动切换到豆包 |
| **豆包调用成功** | 返回结果 |
| **豆包调用失败** | 返回错误提示："服务暂时不可用，请稍后重试" |


### 3.4 图文混排方案

**核心思路**：
1. 文档切分时 → 保证图片链接不被切断
2. 返回回答时 → 图片链接映射为图片和文字一起返回

**技术选型**：

| 组件 | 选择 | 说明 |
|------|------|------|
| **文档切分** | 按段落切分 | 保证图片链接不被切断 |
| **图片链接格式** | Markdown | `![描述](images/xxx.jpg)` |
| **图片显示** | Gradio | 支持渲染 Markdown 图片 |

**实现步骤**：

| 步骤 | 任务 | 说明 |
|------|------|------|
| **1** | 调整切分策略 | 按段落切分，保证图片链接完整 |
| **2** | 测试返回结果 | 确认回答中包含图片链接 |
| **3** | Gradio 显示 | 确认 Gradio 能渲染图片 |

**切分策略调整**（app/rag/loader.py）：

**优点**：

| 优点 | 说明 |
|------|------|
| **简单** | 不需要修改 metadata，不需要复杂逻辑 |
| **可靠** | 图片链接在切分时就保证完整 |
| **兼容** | Gradio 原生支持 Markdown 图片渲染 |

---

## 四、技术选型

### 4.1 大语言模型（LLM）

**选择**：DeepSeek API（主模型）+ 豆包（备用模型）

**选型理由**：

| 维度 | DeepSeek | 豆包 |
|------|----------|------|
| **中文效果** | 优秀，适合实训设备领域的专业术语 | 良好，作为备用 |
| **成本** | 低，按 token 计费 | 低，按 token 计费 |
| **响应速度** | 快 | 快 |
| **稳定性** | 高 | 高 |
| **降级策略** | 主模型，调用失败时切换到豆包 | 备用模型，保证服务可用性 |

---

### 4.2 向量数据库

**选择**：ChromaDB

**选型理由**：

| 维度 | 说明 |
|------|------|
| **部署方式** | 纯 Python、嵌入式、零配置 |
| **学习成本** | 低，Python 原生支持 |
| **适用规模** | 中小规模知识库（几百到几千条） |
| **维护成本** | 低，无需额外服务 |

---

### 4.3 Embedding 模型

**选择**：通义千问 text-embedding-v3

**选型理由**：

| 维度 | 说明 |
|------|------|
| **中文效果** | 优秀，适合实训设备领域的专业术语 |
| **成本** | 低，免费额度充足 |
| **部署方式** | API 调用，无需本地 GPU |
| **兼容性** | 与 DeepSeek LLM 配合使用，技术栈互补 |

---

### 4.4 Web 框架

**选择**：FastAPI

**选型理由**：

| 维度 | 说明 |
|------|------|
| **异步支持** | 原生异步支持，适合高并发场景 |
| **自动文档** | 自动生成 OpenAPI 文档，方便调试 |
| **性能** | 优秀，接近 Node.js |
| **学习成本** | 中等，Python 开发者容易上手 |

---

### 4.5 前端框架

**选择**：Gradio（开发期）

**选型理由**：

| 维度 | 说明 |
|------|------|
| **开发速度** | 快速原型，几行代码出界面 |
| **技术栈** | Python 原生，无需前端开发经验 |
| **适用场景** | 内部工具、快速验证 |
| **扩展性** | 后续可升级为 Vue/React |

---

### 4.6 AI 框架

**选择**：LangChain + LangGraph

**选型理由**：

| 维度 | 说明 |
|------|------|
| **社区** | 丰富，活跃，文档完善 |
| **生态** | 完整，支持 RAG、Agent、HITL |
| **状态管理** | LangGraph 支持状态图，适合复杂工作流 |
| **学习成本** | 中等，有学习曲线 |

---

### 4.7 文档处理

**选择**：MinerU API

**选型理由**：

| 维度 | 说明 |
|------|------|
| **转换质量** | 高，支持 PDF 转 Markdown |
| **图片提取** | 支持提取 PDF 中的图片 |
| **使用方式** | API 调用，简单易用 |
| **成本** | 低，按调用计费 |

---

## 五、核心功能设计

### 5.1 智能路由

**功能目标**：根据用户问题类型，自动分配给对应的专业 Agent 处理。

**关键特性**：

| 特性 | 说明 |
|------|------|
| **LLM 意图分类** | 比关键词规则更灵活，能处理模糊表达 |
| **5 种意图** | 产品咨询、故障排查、培训资料、打招呼、未知 |
| **置信度阈值** | 低于 0.5 时降级为 unknown |
| **共享知识库** | 三个 Agent 用同一个 ChromaDB |

**路由流程**：

```
用户提问 → 意图分类（LLM）→ 路由分发 → Agent 处理 → 返回回答
```

---

### 5.2 Agent 集群

**功能目标**：针对不同领域配置专业 Agent，通过角色扮演和专业 Prompt 提升回答质量。

**关键特性**：

| Agent | 角色定位 | 回答风格 |
|-------|---------|---------|
| **产品专家** | 产品知识专家 | 参数、规格、使用说明 |
| **技术支持** | 技术支持工程师 | 诊断步骤、解决方案 |
| **培训顾问** | 培训顾问 | 实验指导、教学大纲 |

**设计决策**：
- 三个 Agent 共享同一个知识库，靠不同 System Prompt 区分角色
- 技术支持 Agent 使用思维链（CoT）推理，提升故障诊断准确性

---

### 5.3 查询重写

**功能目标**：提升检索效果，解决多轮对话中代词和省略主语问题。

**关键特性**：

| 特性 | 说明 |
|------|------|
| **历史对话感知** | 传入最近 3 轮对话历史，理解上下文 |
| **两步检索** | 原始 query + 改写后 query 分别检索，合并去重 |
| **降级机制** | 改写失败时返回原始 query，不影响系统稳定性 |
| **角色感知** | 不同 Agent 有不同的改写上下文 |

**检索流程**：

```
原始 query ─────→ 检索 1 ──┐
                              ├──→ 合并去重 → top_k
改写后 query ───→ 检索 2 ──┘
```

---

### 5.4 HITL 人工转接

**功能目标**：AI 无法处理时自动转人工，确保服务质量。

**关键特性**：

| 检测类型 | 触发条件 |
|----------|----------|
| **Agent 拒绝** | 回复包含"我不确定"、"建议联系技术支持" |
| **用户主动要求** | 用户说"转人工"、"找客服" |
| **置信度低** | 意图分类置信度 < 0.5 |
| **敏感问题** | 涉及退款、投诉等关键词 |

**技术方案**：基于 LangGraph interrupt 机制，支持流程暂停/恢复。

---

### 5.5 多轮对话记忆

**功能目标**：支持多轮对话，记住上下文，实现连续交互。

**关键特性**：

| 特性 | 说明 |
|------|------|
| **LangGraph State** | 使用 State messages 存储对话历史 |
| **上下文传递** | Agent 接收 messages 参数，拼接到 LLM 输入 |
| **截断策略** | 截取最近 10 条消息，避免 token 溢出 |
| **格式兼容** | 兼容 LangChain 消息对象和字典格式 |
| **会话隔离** | 多用户场景下，每个用户有独立的会话历史 |

**记忆方案**：
- 使用 Gradio history + LangGraph State messages
- Agent 的 run() 方法接收 messages 参数
- 截取最近 10 条消息，避免 token 溢出
- 初期不做 SQLite 持久化，够用

**会话隔离**：
- 使用 session_id 区分不同用户
- 每个用户有独立的 LangGraph State
- 避免不同用户的对话混淆

---

### 5.6 知识库管理

**功能目标**：非技术人员能通过 Web 界面更新知识库内容。

**关键特性**：

| 功能 | 说明 |
|------|------|
| **文档上传** | 支持 PDF 格式，自动调用 MinerU API 转换 |
| **文档删除** | 删除 PDF + Markdown + 向量数据 |
| **文档列表** | 显示已导入文档的状态和统计信息 |
| **增量同步** | 自动检测新增、更新、删除的文件 |

---

### 5.7 图文混排

**功能目标**：回答中包含文档图片，提升用户体验。

**关键特性**：

| 特性 | 说明 |
|------|------|
| **按段落切分** | 保证图片链接不被切断 |
| **Markdown 图片链接** | 使用 `![描述](images/xxx.jpg)` 格式 |
| **Gradio 渲染** | Gradio 原生支持 Markdown 图片渲染 |

---

### 5.8 流式输出

**功能目标**：回答逐字显示，提升用户体验。

**关键特性**：

| 特性 | 说明 |
|------|------|
| **伪流式** | LangGraph 同步处理完成后，逐字符显示回答 |
| **打字效果** | 每个字符间隔 0.02 秒，模拟打字效果 |
| **用户感知** | 看到逐字显示，感觉响应更快 |

**当前方案**：伪流式（同步处理后逐字显示）

**后续优化**：真流式（LLM 边生成边返回）

---

### 5.9 RAG 系统升级（混合检索）

**功能目标**：结合向量检索和关键词检索，提升检索效果。

**关键特性**：

| 特性 | 说明 |
|------|------|
| **向量检索** | 语义匹配，理解模糊表达 |
| **关键词检索** | 精确匹配，适合专业术语 |
| **结果融合** | 两种检索结果合并排序 |
| **适用场景** | 向量检索适合模糊描述，关键词检索适合精确术语 |

**检索流程**：

```
用户查询 → 向量检索 ──┐
                        ├──→ 融合排序 → 结果
              关键词检索 ──┘
```

**后续优化**：
- 使用 rank_bm25 库做关键词检索
- 与向量检索结果融合
- 提升精确术语匹配效果

---

## 六、项目规划

### 6.1 分阶段规划

| 阶段 | 功能 | 时间 |
|------|------|------|
| **Phase 0** | 准备工作（环境搭建、API 测试） | 1-2 天 |
| **Phase 1** | 单 Agent RAG（基础问答） | 3-7 天 |
| **Phase 2** | 多 Agent 路由（意图分类 + Agent 分发） | 8-14 天 |
| **Phase 3** | LangGraph 集成（状态图 + 对话记忆） | 15-21 天 |
| **Phase 4** | 查询重写（历史感知 + 两步检索） | 22-28 天 |
| **Phase 5** | HITL 人工转接（检测 + interrupt） | 29-35 天 |
| **Phase 6** | 知识库管理后台（上传、删除、同步） | 36-42 天 |
| **Phase 7** | 迭代优化（人工转接、图文混排、部署） | 43-56 天 |

**总计**：约 8 周（56 天）

---

### 6.2 Phase 7 迭代计划

| 子阶段 | 功能 | 时间 |
|--------|------|------|
| **Phase 7.1** | 人工转接（简单方案） | 1-2 天 |
| **Phase 7.2** | 图文混排（按段落切分 + Markdown 图片） | 3-5 天 |
| **Phase 7.3** | 云服务器部署（域名 + HTTPS） | 1 周 |
| **Phase 7.4** | 客服管理后台（会话管理 + 回复功能） | 5-7 天 |

---

### 6.3 里程碑

| 里程碑 | 完成时间 | 交付物 |
|--------|----------|--------|
| **M1** | Phase 1 | 能回答产品问题的单 Agent demo |
| **M2** | Phase 2 | 能处理 5 类意图的多 Agent 路由系统 |
| **M3** | Phase 3 | 基于 LangGraph 的多轮对话系统 |
| **M4** | Phase 4 | 历史感知的查询改写 + 两步检索 |
| **M5** | Phase 5 | 基于 LangGraph interrupt 的 HITL 系统 |
| **M6** | Phase 6 | 知识库管理后台 Web 界面 |
| **M7** | Phase 7 | 完整上线系统 |

---

## 七、接口设计

### 7.1 现有接口

**当前已实现的接口**：

| 接口 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 系统信息 | GET | `/` | 返回系统信息 |
| 聊天接口 | POST | `/api/chat` | 基础聊天功能（未使用 LangGraph） |

**现有接口问题**：
- `/api/chat` 是基础版，没有使用 LangGraph 状态图
- 没有流式输出
- 没有 HITL 人工转接
- 没有会话隔离

---

### 7.2 规划接口

**需要新增/改造的接口**：

| 接口 | 方法 | 路径 | 说明 | 优先级 |
|------|------|------|------|--------|
| 对话接口 | POST | `/api/chat` | 改造为使用 LangGraph | ⭐⭐⭐⭐⭐ |
| 流式对话 | POST | `/api/chat/stream` | 流式返回 AI 回复 | ⭐⭐⭐⭐⭐ |
| 文档列表 | GET | `/api/documents` | 获取已导入文档列表 | ⭐⭐⭐⭐ |
| 文档上传 | POST | `/api/documents/upload` | 上传新文档 | ⭐⭐⭐⭐ |
| 文档删除 | DELETE | `/api/documents/{id}` | 删除指定文档 | ⭐⭐⭐⭐ |
| 健康检查 | GET | `/api/health` | 系统健康状态检查 | ⭐⭐⭐ |

---

### 7.3 接口详细设计

#### 7.3.1 对话接口（改造）

**路径**：POST /api/chat

**说明**：改造现有接口，使用 LangGraph 状态图，支持多轮对话、意图分类、HITL 检测。

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| message | string | 是 | 用户消息 |
| session_id | string | 否 | 会话 ID（用于会话隔离） |

**响应参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| reply | string | AI 回复 |
| intent | string | 意图类型（product/fault/training/greeting/unknown） |
| confidence | float | 置信度（0-1） |
| role_name | string | Agent 身份（产品专家/技术支持/培训顾问） |
| sources | list | 参考来源列表 |
| hitl_required | bool | 是否需要人工介入 |

---

#### 7.3.2 流式对话接口（新增）

**路径**：POST /api/chat/stream

**说明**：流式返回 AI 回复，提升用户体验。

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| message | string | 是 | 用户消息 |
| session_id | string | 否 | 会话 ID（用于会话隔离） |

**响应格式**：Server-Sent Events (SSE)

**响应事件**：

| 事件 | 数据 | 说明 |
|------|------|------|
| start | {"agent_role": "产品专家"} | 开始生成 |
| token | {"content": "这"} | 流式 token |
| end | {"sources": [...]} | 生成完成 |

---

#### 7.3.3 文档列表接口（新增）

**路径**：GET /api/documents

**说明**：获取已导入文档列表，从 Gradio 界面迁移。

**请求参数**：无

**响应参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| documents | list | 文档列表 |
| total_files | int | 文档总数 |
| total_chunks | int | chunk 总数 |

**文档对象字段**：

| 字段 | 类型 | 说明 |
|------|------|------|
| filename | string | 文件名 |
| status | string | 状态（success/failed） |
| chunks_count | int | chunk 数量 |
| imported_at | string | 导入时间 |

---

#### 7.3.4 文档上传接口（新增）

**路径**：POST /api/documents/upload

**说明**：上传新文档，从 Gradio 界面迁移。

**请求参数**：

| 参数 | 类型 | 必填 | 说明 |
|------|------|------|------|
| file | file | 是 | PDF 文件 |

**响应参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| success | bool | 是否成功 |
| message | string | 状态消息 |
| filename | string | 文件名 |
| chunks_count | int | chunk 数量 |

---

#### 7.3.5 文档删除接口（新增）

**路径**：DELETE /api/documents/{id}

**说明**：删除指定文档，从 Gradio 界面迁移。

**路径参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| id | string | 文档 ID（文件名） |

**响应参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| success | bool | 是否成功 |
| message | string | 状态消息 |

---

#### 7.3.6 健康检查接口（新增）

**路径**：GET /api/health

**说明**：系统健康状态检查，用于监控和负载均衡。

**响应参数**：

| 参数 | 类型 | 说明 |
|------|------|------|
| status | string | 系统状态（ok/error） |
| version | string | 系统版本 |
| uptime | string | 运行时间 |

---

## 八、部署方案

### 8.1 部署架构

**推荐方案**：单机部署 + Docker

```
┌─────────────────────────────────────────┐
│              云服务器                    │
│  ┌─────────────────────────────────┐   │
│  │  Docker Container               │   │
│  │  ┌─────────────────────────┐   │   │
│  │  │  FastAPI + Gradio       │   │   │
│  │  │  (端口 7860)            │   │   │
│  │  └─────────────────────────┘   │   │
│  │  ┌─────────────────────────┐   │   │
│  │  │  ChromaDB               │   │   │
│  │  │  (本地文件系统)          │   │   │
│  │  └─────────────────────────┘   │   │
│  │  ┌─────────────────────────┐   │   │
│  │  │  知识库文档              │   │   │
│  │  │  (data/raw, processed)  │   │   │
│  │  └─────────────────────────┘   │   │
│  └─────────────────────────────────┘   │
└─────────────────────────────────────────┘
         │
         ↓
┌─────────────────────────────────────────┐
│  用户浏览器                              │
│  http://服务器IP:7860                    │
└─────────────────────────────────────────┘
```

---

### 8.2 服务器要求

| 资源 | 最低配置 | 推荐配置 | 说明 |
|------|----------|----------|------|
| **CPU** | 2 核 | 4 核 | Python 运行需要 |
| **内存** | 4 GB | 8 GB | ChromaDB + LangGraph 需要内存 |
| **磁盘** | 20 GB | 50 GB | 知识库文档 + 向量数据 |
| **网络** | 可访问外网 | 稳定连接 | 调用 DeepSeek/通义千问 API |
| **操作系统** | Linux | Ubuntu 22.04 | Python 环境支持 |

---

### 8.3 部署方式

**推荐**：Docker 部署

**优点**：

| 优点 | 说明 |
|------|------|
| **环境隔离** | 不会与系统其他服务冲突 |
| **易于迁移** | 一次构建，到处运行 |
| **易于维护** | 容器化管理，方便升级 |
| **易于扩展** | 后续可以升级配置 |

---

### 8.4 网络方案

| 阶段 | 方案 | 说明 |
|------|------|------|
| **开发期** | IP 直接访问 | http://服务器IP:7860 |
| **上线后** | 域名 + HTTPS | https://chat.example.com |

---

### 8.5 部署步骤

| 步骤 | 任务 | 说明 |
|------|------|------|
| **1** | 购买云服务器 | 阿里云/腾讯云，4核8G |
| **2** | 安装 Docker | 一键安装 |
| **3** | 编写 Dockerfile | 打包项目 |
| **4** | 上传代码和知识库 | scp / git |
| **5** | 启动容器 | docker-compose up |
| **6** | 配置域名（可选） | 域名解析 + HTTPS |

---

### 8.6 成本估算

| 项目 | 月费用 | 说明 |
|------|--------|------|
| **云服务器** | ¥200-400 | 4核8G |
| **域名** | ¥50-100/年 | 可选 |
| **SSL 证书** | 免费 | Let's Encrypt |
| **总计** | **¥200-400/月** | 取决于配置 |

---

### 8.7 运维注意事项

| 注意事项 | 说明 |
|----------|------|
| **备份** | 定期备份知识库和向量数据 |
| **监控** | 配置健康检查，及时发现问题 |
| **安全** | 配置防火墙，只开放必要端口 |
| **日志** | 配置日志收集，方便排查问题 |

---

## 九、风险管理

### 9.1 技术风险

| 风险 | 影响 | 应对方案 |
|------|------|----------|
| **LLM API 不稳定** | 服务不可用 | 降级策略（DeepSeek + 豆包） |
| **检索效果不佳** | 回答不准确 | 查询重写 + 混合检索 |
| **响应时间过长** | 用户体验差 | 异步处理、缓存热门问答 |
| **数据安全泄露** | 合规风险 | 内网部署、数据加密 |

---

### 9.2 项目风险

| 风险 | 影响 | 应对方案 |
|------|------|----------|
| **知识库内容不足** | 覆盖率低 | 分阶段补充，先覆盖高频问题 |
| **需求变更频繁** | 进度延迟 | 模块化设计，快速迭代 |
| **人员不足** | 开发进度慢 | 优先核心功能，可选功能后续迭代 |
| **预算超支** | 资源不足 | 控制 API 调用成本，优化调用频率 |

---

### 9.3 应对策略

**短期策略**（1-3 个月）：
- 优先覆盖高频问题
- 建立问题反馈机制，持续优化
- 收集用户反馈，调整 Agent Prompt

**中期策略**（3-6 个月）：
- 扩充知识库到 500+ 文档
- 评估是否需要本地化部署
- 优化检索算法，提升准确率

**长期策略**（6-12 个月）：
- 支持多语言（英文等）
- 集成更多外部系统
- 建立知识图谱，提升推理能力

---






