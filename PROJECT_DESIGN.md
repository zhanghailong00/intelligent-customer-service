# 实训设备智能客服系统 — 项目设计文档

## 一、项目背景

### 公司业务
公司制作高校实训道具（嵌入式实验箱），产品包括智能家居、智慧农业等方向的手提式实验箱，主要客户为高校实验室。

### 业务痛点
- 技术支持团队反复回答相同的产品咨询和故障排查问题
- 高校老师使用设备时遇到问题，无法 24 小时获得帮助
- 培训资料分散，查找不方便

### 项目目标
基于多智能体架构，构建一个智能客服系统，覆盖产品咨询、故障排查、培训资料推荐三大场景，支持多轮对话和人工介入审核。

---

## 二、业务场景分析

### 2.1 售前 — 产品咨询
- 箱子里有什么模块？各模块功能是什么？
- 产品规格、参数对比
- 配套的课件、实验指导书、教学大纲

### 2.2 售中 — 交付相关
- 下单流程、配置选择、报价
- 物流跟踪、到货确认
- 验收标准、开箱检查清单

### 2.3 售后 — 使用问题
- 设备故障排查（模块不工作、接线问题）
- 软件问题（编译报错、驱动冲突、环境搭建）
- 技术支持（远程协助、上门服务）
- 退换货/维修流程

### 2.4 其他潜在场景
- 续购/复购（每年采购预算、配件升级）
- 竞赛支持（电子设计大赛、嵌入式竞赛培训）
- 教师社群运营（教学经验交流）

---

## 三、技术架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────┐
│                  前端层                       │
│         Gradio（开发期）/ Vue（上线）          │
└──────────────────┬──────────────────────────┘
                   │ HTTP / WebSocket
┌──────────────────┴──────────────────────────┐
│                 后端服务层                     │
│              FastAPI (Python)                │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────┴──────────────────────────┐
│              智能体编排层                      │
│            LangGraph (图调度引擎)              │
│  ┌──────────┬───────────┬────────────────┐  │
│  │Supervisor│  Agent 1  │    Agent 2 ... │  │
│  │ (路由)   │ (产品知识) │   (故障排查)    │  │
│  └──────────┴───────────┴────────────────┘  │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────┴──────────────────────────┐
│               知识库层                        │
│    ChromaDB(向量存储) + SQLite(结构化数据)     │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────┴──────────────────────────┐
│               基础设施层                      │
│    DeepSeek API + 通义千问 Embedding API     │
└─────────────────────────────────────────────┘
```

### 3.2 数据处理流程

```
实验手册（PDF/图片）
    ↓
技术人员提供的描述文档（文字）
    ↓
文档切分（按问题/步骤/模块切分）
    ↓
向量化 → 存入 ChromaDB
    ↓
同时记录：原图路径、页码、来源
    ↓
用户查询时 → 返回文字 + 原图链接
```

---

## 四、技术选型

### 4.1 大语言模型

| 选项 | 优势 | 劣势 | 适用阶段 |
|---|---|---|---|
| **DeepSeek API**（deepseek-chat） | 模型能力强、中文效果好、价格低 | 需要联网、数据上云 | 当前选择 |
| **Ollama 本地部署**（Qwen2.5/Llama3） | 免费、数据不出公司、响应快 | 需要 GPU 服务器 | 后续备选 |

**当前选择**：DeepSeek API
- DeepSeek-V4-Flash 速度快、成本低，适合客服场景
- 无需 GPU 服务器，降低部署成本
- 后续如需数据不出公司，可迁移到本地 Ollama

### 4.2 向量数据库

**首选：ChromaDB**
- 纯 Python、嵌入式、零配置
- 单机足够支撑中小型知识库（几百到几千条）

备选：Milvus / Weaviate（数据量百万级时考虑）

### 4.3 Embedding 模型

**当前选择：通义千问 text-embedding-v3**

| 选项 | 说明 | 选择原因 |
|---|---|---|
| **通义千问 text-embedding-v3** | 中文效果好，API 调用 | ✅ 当前选择 |
| BAAI/bge-large-zh | 开源，可本地部署 | 备选（显存不足时不推荐） |

**选择理由**：
- 中文向量化效果优秀，适合实训设备领域的专业术语
- API 调用无需本地 GPU，节省资源
- 与 DeepSeek LLM 配合使用，技术栈互补
- 免费额度充足，开发成本低

### 4.4 文档处理

| 工具 | 用途 |
|---|---|
| LangChain TextLoader | 加载 TXT/Markdown |
| PyPDFLoader | 加载 PDF |
| RecursiveCharacterTextSplitter | 文档切分 |
| PaddleOCR | 提取图片中的文字标签 |

### 4.5 前端

| 阶段 | 方案 |
|---|---|
| 开发期 | Gradio（Python 写、几行代码出界面） |
| 上线后 | Vue3 + Element Plus 或继续用 Gradio |

### 4.6 核心依赖

```
langchain>=0.2
langgraph>=0.2
langchain-community
langchain-openai
chromadb
fastapi
uvicorn
gradio
pypdf
loguru
python-dotenv
```

---

## 五、项目目录结构

```
smart-service/
├── app/
│   ├── main.py              # FastAPI 入口
│   ├── config.py            # 配置管理
│   ├── llm/
│   │   ├── __init__.py
│   │   └── models.py        # 模型封装
│   ├── graph/
│   │   ├── __init__.py
│   │   ├── state.py          # 状态定义
│   │   ├── supervisor.py     # 路由分发
│   │   └── builder.py        # 图构建
│   ├── agents/
│   │   ├── __init__.py
│   │   ├── product.py        # 产品知识 Agent
│   │   ├── fault.py          # 故障排查 Agent
│   │   └── training.py       # 培训资料 Agent
│   ├── rag/
│   │   ├── __init__.py
│   │   ├── loader.py         # 文档加载
│   │   ├── splitter.py       # 文档切分
│   │   ├── retriever.py      # 检索器
│   │   └── vectorstore.py    # 向量库管理
│   ├── query/
│   │   ├── __init__.py
│   │   └── rewriter.py       # 查询重写模块
│   ├── memory/
│   │   ├── __init__.py
│   │   └── manager.py        # 对话记忆管理
│   ├── hitl/
│   │   ├── __init__.py
│   │   └── reviewer.py       # HITL 审核逻辑
│   ├── api/
│   │   ├── __init__.py
│   │   └── routes.py         # API 路由
│   └── db/
│       ├── __init__.py
│       └── models.py         # SQLite 数据模型
├── web/
│   └── app.py                # Gradio 界面
├── knowledge/                # 知识库文档存放
│   ├── products/             # 产品手册
│   ├── faq/                  # 常见问题
│   └── training/             # 培训资料
├── tests/
├── docker/
│   ├── Dockerfile
│   └── docker-compose.yml
├── requirements.txt
└── README.md
```

---

## 六、核心功能设计

### 6.1 Supervisor 路由分发

基于用户意图，将任务分发给不同的 Agent：

```python
# 意图分类规则
- recommend → 产品知识 Agent（产品咨询、功能介绍、参数对比）
- customer_service → 故障排查 Agent（设备故障、软件问题、连接问题）
- training → 培训资料 Agent（课件、实验指导、教学大纲）
```

### 6.2 查询重写模块

用户提问通常不够专业，需要改写为精确的技术查询：

```
用户："传感器不亮了"
    ↓
改写后："温湿度传感器指示灯不工作，可能原因和排查步骤"
关键词：传感器、指示灯、故障、排查
```

改写策略根据意图类型差异化：

| 意图类型 | 改写策略 | 示例 |
|---|---|---|
| 产品咨询 | 补充产品名称、模块名称 | "箱子有什么" → "智慧农业实训箱包含哪些模块" |
| 故障排查 | 补充故障现象、设备部件 | "连不上网" → "实训箱WiFi模块连接失败排查" |
| 培训资料 | 补充资料类型、课程名称 | "有没有课件" → "智慧农业实训箱配套教学课件" |

### 6.3 RAG 检索增强

```
改写后的查询 → ChromaDB 向量检索 → Top-K 相关文档
    ↓
LLM 结合检索结果 + 对话历史 → 生成回答
    ↓
返回：文字回答 + 对应原图（如果有）
```

### 6.4 多轮对话记忆

| 记忆类型 | 存储方式 | 用途 |
|---|---|---|
| 短期记忆 | 内存（当前会话） | 保持上下文连贯 |
| 长期记忆 | SQLite（按用户ID） | 记住用户历史偏好 |

### 6.5 HITL 人工审核

当 Agent 无法确定回答或涉及敏感操作时，触发人工审核：

```
Agent 判断需要人工介入
    ↓
LangGraph interrupt 机制暂停
    ↓
通知人工审核员（企业微信/后台）
    ↓
审核员确认/修改回答
    ↓
继续执行流程
```

---

## 七、LangGraph 状态定义

```python
from typing import TypedDict, Annotated
from langgraph.graph import add_messages

class State(TypedDict):
    messages: Annotated[list, add_messages]   # 对话历史
    type: str                                  # 意图分类结果
    phase: str                                 # 当前阶段（dispatch/gather/done）
    query_rewrite: str                         # 改写后的查询
    retrieved_docs: list                       # 检索到的文档
    images: list                               # 相关图片路径
    hitl_required: bool                        # 是否需要人工审核
```

---

## 八、开发路线图

### 8.1 开发策略

**核心原则**：
1. **先验证价值，再优化技术** — 不要一上来就搭复杂架构
2. **能用 API 就不本地部署** — 省时间、省资源
3. **先手动后自动** — 能人工处理的先人工，确认流程后再自动化
4. **每天要有可见产出** — 保持动力，方便调整方向

**关键决策**：
- 初期用**规则路由**而非 LLM 意图分类（简单、可控、无延迟）
- 初期用**内存存储**对话历史（SQLite 增加复杂度，初期用户少）
- **LangGraph** 仅在需要复杂状态流转或人工介入时引入

---

### Phase 0：准备工作（第 1-2 天）

**目标**：环境就绪，能调通 API

```
├── 0.1 注册 DeepSeek API + 通义千问 Embedding API
├── 0.2 测试 API 调用是否正常
├── 0.3 准备 3-5 个产品文档样例（PDF/Word）
├── 0.4 搭建项目骨架：FastAPI + Gradio
└── 0.5 编写 .env 配置文件，管理 API Key

交付物：能通过 API 问答的简单 demo
```

**验证标准**：输入问题，能返回 LLM 生成的回答

---

### Phase 1：单 Agent RAG（第 3-7 天）

**目标**：一个 Agent 能回答产品问题

```
├── 1.1 实现文档加载（PyPDFLoader）
├── 1.2 实现文档切分（RecursiveCharacterTextSplitter）
├── 1.3 实现向量存储（通义千问 Embedding + ChromaDB）
├── 1.4 实现检索器（向量检索 Top-K）
├── 1.5 实现 RAG 问答链（检索 + LLM 生成）
├── 1.6 Gradio 界面集成
└── 1.7 测试：准备 10 个常见问题，验证回答质量

交付物：能回答产品问题的单 Agent demo
```

**验证标准**：
- 能从知识库中检索到相关内容
- 回答基于检索结果，而非胡编乱造
- 响应时间 < 3 秒

**为什么先做这个？**
- 多智能体的前提是每个 Agent 都能独立工作
- 先验证 RAG 效果，再决定是否需要多 Agent
- 快速拿到可演示的成果

---

### Phase 2：意图路由（第 8-14 天）

**目标**：根据问题类型分发给不同处理逻辑

```
├── 2.1 设计意图分类规则（关键词匹配）
├── 2.2 实现产品知识 Agent（基于 Phase 1 的 RAG）
├── 2.3 实现故障排查 Agent（不同的 prompt 模板）
├── 2.4 实现培训资料 Agent（检索培训文档）
├── 2.5 实现路由器（根据意图分发）
├── 2.6 Gradio 界面支持选择问题类型
└── 2.7 测试：每类问题各 5 个，验证路由准确性

交付物：能处理 3 类问题的简单路由系统
```

**验证标准**：
- 产品咨询 → 产品知识 Agent
- 故障排查 → 故障排查 Agent
- 培训资料 → 培训资料 Agent
- 路由准确率 > 90%

**为什么用规则而不是 LLM 路由？**
- 规则简单、可控、无延迟
- LLM 路由需要训练数据，初期没有
- 先用规则跑通，后续再升级为 LLM 意图分类

---

### Phase 3：对话记忆（第 15-21 天）

**目标**：支持多轮对话

```
├── 3.1 实现内存存储对话历史
├── 3.2 实现上下文拼接（历史 + 当前问题）
├── 3.3 处理上下文窗口溢出（截断策略）
├── 3.4 Gradio 界面支持对话历史显示
└── 3.5 测试：连续问 5-10 个相关问题，验证上下文连贯性

交付物：支持 5-10 轮对话的 demo
```

**验证标准**：
- 能记住之前说过的话
- 回答能引用之前的上下文
- 不会出现上下文混乱

**为什么不做长期记忆？**
- 初期用户量少，不需要
- SQLite 存储增加复杂度
- 先验证多轮对话的价值，再决定是否需要持久化

---

### Phase 4：查询重写（第 22-28 天）

**目标**：提升检索效果

```
├── 4.1 收集用户口语化提问样本（20-30 条）
├── 4.2 设计查询重写 prompt 模板
├── 4.3 实现查询重写模块（LLM 改写）
├── 4.4 对比重写前后的检索效果
├── 4.5 优化重写策略
└── 4.6 测试：准备 10 个口语化问题，验证重写效果

交付物：检索效果提升 20-30%
```

**验证标准**：
- 重写后的查询更专业化
- 检索召回率提升
- 回答质量明显改善

**为什么放在后面做？**
- 需要先有 RAG 基线，才能评估重写效果
- 没有真实数据时，重写规则很难设计
- 先用原始查询跑，收集 bad case 后再优化

---

### Phase 5：HITL 机制（第 29-35 天）

**目标**：复杂问题能转人工

```
├── 5.1 设计置信度评估规则
├── 5.2 实现低置信度检测（关键词 + 规则）
├── 5.3 实现转人工逻辑（返回提示信息）
├── 5.4 Gradio 界面支持人工审核标记
└── 5.5 测试：准备 5 个复杂问题，验证转人工准确性

交付物：能识别需要人工介入的问题
```

**验证标准**：
- 能识别出不确定的回答
- 给出合理的转人工提示
- 不会误判正常问题

**为什么不做 LangGraph interrupt？**
- interrupt 机制复杂，需要状态持久化
- 初期用户少，人工审核量不大
- 先用简单判断，后续再升级

---

### Phase 6：知识库后台（第 36-42 天）

**目标**：非技术人员能更新知识库

```
├── 6.1 实现文档上传 API
├── 6.2 实现自动文档处理（切分 + 向量化）
├── 6.3 实现知识库管理界面（Gradio）
├── 6.4 实现文档删除/更新功能
└── 6.5 测试：上传新文档，验证能被检索到

交付物：Web 界面上传文档
```

**验证标准**：
- 上传 PDF/Word 能自动处理
- 新文档能被检索和问答
- 操作简单，非技术人员能用

**为什么放到最后？**
- 前期知识库内容少，手动更新够用
- 先验证系统价值，再投入做后台
- 后台是锦上添花，不是核心功能

---

### Phase 7：部署上线（第 43-49 天）

**目标**：公司内部可用

```
├── 7.1 Docker 化部署
├── 7.2 Nginx 反向代理 + HTTPS
├── 7.3 性能优化（缓存、并发）
├── 7.4 编写使用文档
├── 7.5 制作演示视频
└── 7.6 撰写简历话术

交付物：可部署的完整项目
```

---

### 8.2 关键决策点

**什么时候该做多智能体？**

| 条件 | 决策 |
|---|---|
| 单 Agent 能回答 80% 的问题 | 不需要多智能体 |
| 不同类型问题需要不同处理逻辑 | 需要路由 |
| 单 Agent 响应变慢或 prompt 变长 | 需要拆分 |

**实际情况**：很可能单 Agent + 规则路由就够了

**什么时候需要 LangGraph？**

| 条件 | 决策 |
|---|---|
| 需要复杂的状态流转 | 用 LangGraph |
| 需要人工介入暂停/恢复 | 用 LangGraph |
| 简单的线性流程 | 不需要 |

**实际情况**：初期用普通函数调用，后期再迁移

---

### 8.3 时间规划

| 阶段 | 周期 | 产出 |
|---|---|---|
| Phase 0 | 第 1-2 天 | API 调通 demo |
| Phase 1 | 第 3-7 天 | 单 Agent RAG demo |
| Phase 2 | 第 8-14 天 | 多类型问题路由 |
| Phase 3 | 第 15-21 天 | 多轮对话支持 |
| Phase 4 | 第 22-28 天 | 查询重写优化 |
| Phase 5 | 第 29-35 天 | HITL 机制 |
| Phase 6 | 第 36-42 天 | 知识库后台 |
| Phase 7 | 第 43-49 天 | 部署上线 |

**总计**：约 7 周（50 天）

**快速方案**（4 周内完成）：
- Phase 0 + Phase 1 + Phase 2 + Gradio 界面
- 产出：能演示的 demo，够写简历

---

## 九、简历话术参考

### 项目名称
**智能客服系统** — 基于 LangGraph 多智能体架构的企业级实训设备服务平台

### 技术描述
- 设计 Supervisor 模式的多智能体协作架构，实现产品咨询、故障排查、培训资料三类任务的自动路由与分发
- 基于 RAG 架构实现知识库问答，采用 ChromaDB 向量数据库，支持语义检索与关键词混合搜索
- 实现查询重写模块，针对非专业用户的口语化提问，通过意图分类驱动的差异化改写策略，提升检索召回率
- 实现 HITL（Human-in-the-Loop）机制，对复杂/敏感问题自动触发人工审核流程，确保回答准确性
- 设计多轮对话记忆管理方案，支持会话级短期记忆与用户级长期记忆，实现上下文连续的智能交互
- 开发知识库管理后台，支持非技术人员通过 Web 界面动态更新知识库内容

### 技术栈
Python / LangChain / LangGraph / FastAPI / ChromaDB / Gradio / DeepSeek API / 通义千问 Embedding / SQLite

---

## 十、待确认事项

| 序号 | 事项 | 负责人 | 状态 |
|---|---|---|---|
| 1 | 技术人员描述文档的格式（Word/Excel/其他） | 待确认 | |
| 2 | 图片和描述的对应关系（每张图一段描述/按章节整理） | 待确认 | |
| 3 | 图片文件名命名规则 | 待确认 | |
| 4 | 公司是否有 GPU 服务器 | 待确认 | |
| 5 | DeepSeek API Key 是否可用 | 待确认 | |
| 6 | 通义千问 Embedding API Key 是否可用 | 待确认 | |
| 7 | 公司内部通讯工具（企业微信/飞书/钉钉） | 待确认 | |
| 8 | 真实用户问答样本收集（50-100 条） | 进行中 | |

---

## 十一、风险与应对

| 风险 | 应对方案 |
|---|---|
| 公司没有 GPU 服务器 | 使用 DeepSeek API，无需 GPU；后续如需数据不出公司可迁移本地模型 |
| 产品资料格式混乱 | 用 unstructured 库处理，必要时人工整理 |
| 一个人进度慢 | Phase 1 快速出 demo 给领导看，争取资源 |
| 效果不够好 | 先覆盖高频问题，逐步优化 |
| 用户提问太口语化 | 查询重写模块 + 人工标注的高质量数据 |
