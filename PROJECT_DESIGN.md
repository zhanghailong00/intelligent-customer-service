# 实训设备智能客服系统 — 项目设计文档

## 一、项目背景

### 公司业务
公司制作高校实训道具（嵌入式实验箱），产品包括智能家居、智慧农业等方向的手提式实验箱，主要客户为高校实验室。

### 业务痛点
- 技术支持团队反复回答相同的产品咨询和故障排查问题
- 高校老师使用设备时遇到问题，无法 24 小时获得帮助
- 培训资料分散，查找不方便

### 项目目标
基于多智能体架构，构建一个智能客服系统，覆盖产品咨询、故障排查、培训资料推荐三大场景，支持多轮对话和人工介入审核。

---

## 二、业务场景分析

### 2.1 售前 — 产品咨询
- 箱子里有什么模块？各模块功能是什么？
- 产品规格、参数对比
- 配套的课件、实验指导书、教学大纲

### 2.2 售中 — 交付相关
- 下单流程、配置选择、报价
- 物流跟踪、到货确认
- 验收标准、开箱检查清单

### 2.3 售后 — 使用问题
- 设备故障排查（模块不工作、接线问题）
- 软件问题（编译报错、驱动冲突、环境搭建）
- 技术支持（远程协助、上门服务）
- 退换货/维修流程

### 2.4 其他潜在场景
- 续购/复购（每年采购预算、配件升级）
- 竞赛支持（电子设计大赛、嵌入式竞赛培训）
- 教师社群运营（教学经验交流）

---

## 三、技术架构

### 3.1 整体架构图

```
┌─────────────────────────────────────────────┐
│                  前端层                       │
│         Gradio（开发期）/ Vue（上线）          │
└──────────────────┬──────────────────────────┘
                   │ HTTP / WebSocket
┌──────────────────┴──────────────────────────┐
│                 后端服务层                     │
│              FastAPI (Python)                │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────┴──────────────────────────┐
│              智能体编排层                      │
│            LangGraph (图调度引擎)              │
│  ┌──────────┬───────────┬────────────────┐  │
│  │Supervisor│  Agent 1  │    Agent 2 ... │  │
│  │ (路由)   │ (产品知识) │   (故障排查)    │  │
│  └──────────┴───────────┴────────────────┘  │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────┴──────────────────────────┐
│               知识库层                        │
│    ChromaDB(向量存储) + SQLite(结构化数据)     │
└──────────────────┬──────────────────────────┘
                   │
┌──────────────────┴──────────────────────────┐
│               基础设施层                      │
│    DeepSeek API + 通义千问 Embedding API     │
└─────────────────────────────────────────────┘
```

### 3.2 数据处理流程

```
原始资料（PDF/Word/图片）
    ↓
转换为 Markdown 格式（统一格式，保留结构）
    ↓
文档切分（按标题/步骤/模块切分）
    ↓
向量化 → 存入 ChromaDB
    ↓
同时记录：图片路径、章节编号、来源
    ↓
用户查询时 → 返回文字 + 图片
```

**为什么选择 Markdown 格式？**

| 原因 | 说明 |
|---|---|
| 结构清晰 | 标题层级明确，易于切分 |
| 图片支持 | 用 `![](path)` 引用图片，前端直接显示 |
| 解析简单 | 比 PDF/Word 更容易程序化处理 |
| 版本友好 | Git diff 可读，便于协作 |
| 格式统一 | 所有资料转为同一种格式，降低处理复杂度 |

**资料处理要求**：
- 所有产品手册、实验指导书等资料必须先转为 Markdown 格式
- 图片保存到 `knowledge/images/` 目录，Markdown 中用相对路径引用
- 保留原始标题层级（# ## ### ####），便于切分

---

## 四、技术选型

### 4.1 大语言模型

| 选项 | 优势 | 劣势 | 适用阶段 |
|---|---|---|---|
| **DeepSeek API**（deepseek-chat） | 模型能力强、中文效果好、价格低 | 需要联网、数据上云 | 当前选择 |
| **Ollama 本地部署**（Qwen2.5/Llama3） | 免费、数据不出公司、响应快 | 需要 GPU 服务器 | 后续备选 |

**当前选择**：DeepSeek API
- DeepSeek-V4-Flash 速度快、成本低，适合客服场景
- 无需 GPU 服务器，降低部署成本
- 后续如需数据不出公司，可迁移到本地 Ollama

### 4.2 向量数据库

**首选：ChromaDB**
- 纯 Python、嵌入式、零配置
- 单机足够支撑中小型知识库（几百到几千条）

备选：Milvus / Weaviate（数据量百万级时考虑）

### 4.3 Embedding 模型

**当前选择：通义千问 text-embedding-v3**

| 选项 | 说明 | 选择原因 |
|---|---|---|
| **通义千问 text-embedding-v3** | 中文效果好，API 调用 | ✅ 当前选择 |
| BAAI/bge-large-zh | 开源，可本地部署 | 备选（显存不足时不推荐） |

**选择理由**：
- 中文向量化效果优秀，适合实训设备领域的专业术语
- API 调用无需本地 GPU，节省资源
- 与 DeepSeek LLM 配合使用，技术栈互补
- 免费额度充足，开发成本低

### 4.4 文档处理

**文档格式要求**：所有资料必须先转换为 Markdown 格式

| 工具 | 用途 |
|---|---|
| LangChain TextLoader | 加载 Markdown 文档 |
| MarkdownHeaderTextSplitter | 按标题层级切分 |
| RecursiveCharacterTextSplitter | 按步骤/段落切分 |
| PyMuPDF | 从 PDF 提取图片（转换时使用） |

**转换流程**：
```
PDF/Word → Markdown（人工或工具转换）
         → 图片提取到 knowledge/images/
         → Markdown 中用相对路径引用图片
```

### 4.5 前端

| 阶段 | 方案 |
|---|---|
| 开发期 | Gradio（Python 写、几行代码出界面） |
| 上线后 | Vue3 + Element Plus 或继续用 Gradio |

### 4.6 核心依赖

```
# LangChain v1.0
langchain-core>=1.0
langchain-openai>=1.0
langgraph>=1.0

# 向量数据库
chromadb

# Web 框架
fastapi
uvicorn
gradio

# 文档处理
pymupdf          # PDF 图片提取

# AI 服务
dashscope        # 通义千问 Embedding
openai           # DeepSeek API（兼容 OpenAI）

# 工具
loguru
python-dotenv
```

---

## 五、项目目录结构

```
smart-service/
├── app/
│   ├── main.py              # FastAPI 入口
│   ├── config.py            # 配置管理
│   ├── llm/
│   │   ├── __init__.py
│   │   └── models.py        # 模型封装
│   ├── graph/
│   │   ├── __init__.py
│   │   ├── state.py          # 状态定义
│   │   ├── supervisor.py     # 路由分发
│   │   └── builder.py        # 图构建
│   ├── agents/
│   │   ├── __init__.py
│   │   ├── product.py        # 产品知识 Agent
│   │   ├── fault.py          # 故障排查 Agent
│   │   └── training.py       # 培训资料 Agent
│   ├── rag/
│   │   ├── __init__.py
│   │   ├── loader.py         # 文档加载
│   │   ├── splitter.py       # 文档切分
│   │   ├── retriever.py      # 检索器
│   │   └── vectorstore.py    # 向量库管理
│   ├── query/
│   │   ├── __init__.py
│   │   └── rewriter.py       # 查询重写模块
│   ├── memory/
│   │   ├── __init__.py
│   │   └── manager.py        # 对话记忆管理
│   ├── hitl/
│   │   ├── __init__.py
│   │   └── reviewer.py       # HITL 审核逻辑
│   ├── api/
│   │   ├── __init__.py
│   │   └── routes.py         # API 路由
│   └── db/
│       ├── __init__.py
│       └── models.py         # SQLite 数据模型
├── web/
│   └── app.py                # Gradio 界面
├── knowledge/                # 知识库文档存放（Markdown 格式）
│   ├── products/             # 产品手册（.md 文件）
│   ├── faq/                  # 常见问题（.md 文件）
│   ├── training/             # 培训资料（.md 文件）
│   └── images/               # 图片文件（从 PDF 提取）
├── tests/
├── tests/
├── docker/
│   ├── Dockerfile
│   └── docker-compose.yml
├── requirements.txt
└── README.md
```

---

## 六、核心功能设计

### 6.1 Supervisor 路由分发

基于用户意图，将任务分发给不同的 Agent：

```python
# 意图分类规则
- recommend → 产品知识 Agent（产品咨询、功能介绍、参数对比）
- customer_service → 故障排查 Agent（设备故障、软件问题、连接问题）
- training → 培训资料 Agent（课件、实验指导、教学大纲）
```

### 6.2 查询重写模块

用户提问通常不够专业，需要改写为精确的技术查询：

```
用户："传感器不亮了"
    ↓
改写后："温湿度传感器指示灯不工作，可能原因和排查步骤"
关键词：传感器、指示灯、故障、排查
```

改写策略根据意图类型差异化：

| 意图类型 | 改写策略 | 示例 |
|---|---|---|
| 产品咨询 | 补充产品名称、模块名称 | "箱子有什么" → "智慧农业实训箱包含哪些模块" |
| 故障排查 | 补充故障现象、设备部件 | "连不上网" → "实训箱WiFi模块连接失败排查" |
| 培训资料 | 补充资料类型、课程名称 | "有没有课件" → "智慧农业实训箱配套教学课件" |

### 6.3 RAG 检索增强

```
改写后的查询 → ChromaDB 向量检索 → Top-K 相关文档
    ↓
LLM 结合检索结果 + 对话历史 → 生成回答
    ↓
返回：文字回答 + 对应原图（如果有）
```

### 6.4 多轮对话记忆

| 记忆类型 | 存储方式 | 用途 |
|---|---|---|
| 短期记忆 | 内存（当前会话） | 保持上下文连贯 |
| 长期记忆 | SQLite（按用户ID） | 记住用户历史偏好 |

### 6.5 HITL 人工审核

当 AI 无法独立完成任务时，自动转交人工客服处理。

**状态图变化**：
```
现在：
classifier → agent → end

加 HITL 后：
classifier → agent → hitl_checker →
  ├── 不需要人工 → end
  └── 需要人工 → interrupt（暂停）→ 等待人工输入 → end
```

**触发场景**：

| 优先级 | 场景 | 触发条件 | 实现方式 |
|--------|------|---------|---------|
| 必做 | Agent 无法回答 | 回复包含"我不确定"/"建议联系技术支持" | 关键词匹配 |
| 必做 | 用户主动要求 | "转人工"/"找客服"/"人工客服" | 关键词匹配 |
| 必做 | 置信度低 | `confidence < 0.5` | 阈值判断 |
| 可选 | 敏感问题 | 退款/投诉/法律/赔偿 | 关键词匹配 |
| 可选 | 情绪波动 | 愤怒/不满 | 情感分析（LLM 或规则） |
| 可选 | 重复提问 | 同一问题问了 3 次以上 | 计数器 |

---

## 七、LangGraph 状态定义

```python
from typing import TypedDict, Annotated, List
from langgraph.graph import add_messages

class State(TypedDict):
    # 对话历史（LangGraph 自动管理）
    messages: Annotated[list, add_messages]

    # 意图分类结果（classifier 节点写入）
    intent: str          # product/fault/training/greeting/unknown
    confidence: float    # 0.0 - 1.0

    # Agent 身份名称（classifier 节点写入，用于 UI 显示）
    role_name: str       # 产品专家/技术支持/培训顾问

    # 生成的回答（agent 节点或 greeting/unknown 节点写入）
    answer: str

    # 参考来源（agent 节点写入）
    sources: List[str]

    # 是否需要人工介入（hitl_checker 节点写入）
    hitl_required: bool
```

---

## 八、开发路线图

### 8.1 开发策略

**核心原则**：
1. **先验证价值，再优化技术** — 不要一上来就搭复杂架构
2. **能用 API 就不本地部署** — 省时间、省资源
3. **先手动后自动** — 能人工处理的先人工，确认流程后再自动化
4. **每天要有可见产出** — 保持动力，方便调整方向

**关键决策**：
- 已完成：LLM 意图分类（比规则更灵活）
- 已完成：LangGraph 状态图驱动路由（比 if-else 更可维护）
- 已完成：内存存储对话历史（Gradio + LangGraph State）
- 进行中：HITL 人工介入（LangGraph interrupt）
- 待做：查询重写（有评估才能衡量效果）

---

### Phase 0：准备工作（第 1-2 天）

**目标**：环境就绪，能调通 API

```
├── 0.1 注册 DeepSeek API + 通义千问 Embedding API
├── 0.2 测试 API 调用是否正常
├── 0.3 准备 3-5 个产品文档样例（PDF/Word）
├── 0.4 搭建项目骨架：FastAPI + Gradio
└── 0.5 编写 .env 配置文件，管理 API Key

交付物：能通过 API 问答的简单 demo
```

**验证标准**：输入问题，能返回 LLM 生成的回答

---

### Phase 1：单 Agent RAG（第 3-7 天）

**目标**：一个 Agent 能回答产品问题

```
├── 1.1 实现文档加载（PyPDFLoader）
├── 1.2 实现文档切分（RecursiveCharacterTextSplitter）
├── 1.3 实现向量存储（通义千问 Embedding + ChromaDB）
├── 1.4 实现检索器（向量检索 Top-K）
├── 1.5 实现 RAG 问答链（检索 + LLM 生成）
├── 1.6 Gradio 界面集成
└── 1.7 测试：准备 10 个常见问题，验证回答质量

交付物：能回答产品问题的单 Agent demo
```

**验证标准**：
- 能从知识库中检索到相关内容
- 回答基于检索结果，而非胡编乱造
- 响应时间 < 3 秒

**为什么先做这个？**
- 多智能体的前提是每个 Agent 都能独立工作
- 先验证 RAG 效果，再决定是否需要多 Agent
- 快速拿到可演示的成果

---

### Phase 2：意图路由（第 8-14 天）✅ 已完成

**目标**：根据问题类型分发给不同处理逻辑

```
├── 2.1 实现 LLM 意图分类器（少样本提示 + 边界判断规则）
├── 2.2 实现 Agent 基类（共享 RAG 检索 + LLM 调用）
├── 2.3 实现产品知识 Agent（角色扮演 + 少样本 + 拒绝策略）
├── 2.4 实现故障排查 Agent（角色扮演 + 少样本 + 拒绝策略 + 思维链）
├── 2.5 实现培训资料 Agent（角色扮演 + 少样本 + 拒绝策略）
├── 2.6 实现路由器（意图分类 → Agent 分发 + greeting/unknown 处理）
├── 2.7 Prompt 工程优化（Few-shot、CoT、拒绝策略）
└── 2.8 Gradio 界面集成 + Agent 身份标签显示

交付物：能处理 5 类意图的多 Agent 路由系统
```


**验证标准**：
- 产品咨询 → 产品专家 Agent
- 故障排查 → 技术支持 Agent（思维链分步排查）
- 培训资料 → 培训顾问 Agent
- 打招呼 → 友好回复 + 功能引导
- 无关问题 → 通用回复，不检索知识库
- 路由准确率 > 90%

**设计决策**：
- 使用 LLM 意图分类而不是关键词规则：LLM 能理解模糊表达，比如"箱子坏了"也能正确分类
- 共享知识库而不是多个 collection：简单高效，靠不同 System Prompt 区分角色
- 先用 if-else 路由：简单可控，Phase 3 引入 LangGraph 时再重构

---

### Phase 3：LangGraph 集成 + 对话记忆（第 15-21 天）

**目标**：引入 LangGraph 框架，支持多轮对话

```
├── 3.1 定义 LangGraph State（messages, intent, agent_role, hitl_required 等）
├── 3.2 构建状态图（意图分类 → Agent 分发 → 回复生成）
├── 3.3 实现对话记忆（LangGraph State 天然支持 messages 列表）
├── 3.4 处理上下文窗口溢出（截断策略）
├── 3.5 将现有 if-else 路由迁移到 LangGraph 状态图
├── 3.6 Gradio 界面适配 LangGraph 流程
└── 3.7 测试：连续问 5-10 个相关问题，验证上下文连贯性

交付物：基于 LangGraph 的多轮对话系统
```

**验证标准**：
- 能记住之前说过的话
- 回答能引用之前的上下文
- 不会出现上下文混乱
- 状态图可视化（LangGraph Studio）

**为什么现在引入 LangGraph？**
- Phase 5 需要 HITL（人工介入），LangGraph 的 `interrupt` 机制是最佳方案
- 对话记忆用 LangGraph State 管理，比自己造轮子更可靠
- 状态图比 if-else 可维护，后续加查询重写、情感检测等节点更方便
- 面试时能聊 LangGraph 架构设计

---

### Phase 4：查询重写（第 22-28 天）

**目标**：提升检索效果

```
├── 4.1 收集用户口语化提问样本（20-30 条）
├── 4.2 设计查询重写 prompt 模板
├── 4.3 实现查询重写模块（LLM 改写）
├── 4.4 对比重写前后的检索效果
├── 4.5 优化重写策略
└── 4.6 测试：准备 10 个口语化问题，验证重写效果

交付物：检索效果提升 20-30%
```

**验证标准**：
- 重写后的查询更专业化
- 检索召回率提升
- 回答质量明显改善

**为什么放在后面做？**
- 需要先有 RAG 基线，才能评估重写效果
- 没有真实数据时，重写规则很难设计
- 先用原始查询跑，收集 bad case 后再优化

---

### Phase 5：HITL 机制（第 29-35 天）

**目标**：AI 无法处理时自动转人工

**实现方案**：基于 LangGraph interrupt 的 HITL

```
├── 5.1 实现 hitl_checker 节点（核心检测逻辑）
│   ├── 5.1.1 Agent 拒绝检测（关键词匹配）
│   ├── 5.1.2 用户主动转人工检测（关键词匹配）
│   ├── 5.1.3 置信度检测（阈值判断）
│   └── 5.1.4 敏感问题检测（关键词匹配，可选）
├── 5.2 集成 LangGraph interrupt 机制
│   ├── 5.2.1 hitl_checker 调用 interrupt() 暂停图执行
│   ├── 5.2.2 MemorySaver 持久化状态
│   └── 5.2.3 人工输入后恢复图执行
├── 5.3 Gradio 界面适配
│   ├── 5.3.1 显示"转人工"提示
│   └── 5.3.2 模拟人工审核按钮
└── 5.4 测试：准备 10 个触发 HITL 的问题

交付物：基于 LangGraph interrupt 的 HITL 系统
```

**技术方案**：

| 组件 | 实现 |
|------|------|
| 触发检测 | hitl_checker 节点，检查 3 个必做条件 |
| 图暂停/恢复 | LangGraph `interrupt()` + `MemorySaver` |
| Gradio 交互 | 显示"转人工"提示，模拟审核按钮 |

**验证标准**：
- 能识别出不确定的回答
- 给出合理的转人工提示
- 不会误判正常问题
- LangGraph interrupt 能正确暂停/恢复流程

**为什么用 LangGraph interrupt？**
- 天然支持流程暂停/恢复，不需要自己管理状态
- 可以在暂停时持久化对话状态，恢复后继续执行
- 面试时能聊 LangGraph 的 HITL 设计

---

### Phase 6：知识库后台（第 36-42 天）

**目标**：非技术人员能更新知识库

```
├── 6.1 实现文档上传 API
├── 6.2 实现自动文档处理（切分 + 向量化）
├── 6.3 实现知识库管理界面（Gradio）
├── 6.4 实现文档删除/更新功能
└── 6.5 测试：上传新文档，验证能被检索到

交付物：Web 界面上传文档
```

**验证标准**：
- 上传 PDF/Word 能自动处理
- 新文档能被检索和问答
- 操作简单，非技术人员能用

**为什么放到最后？**
- 前期知识库内容少，手动更新够用
- 先验证系统价值，再投入做后台
- 后台是锦上添花，不是核心功能

---

### Phase 7：部署上线（第 43-49 天）

**目标**：公司内部可用

```
├── 7.1 Docker 化部署
├── 7.2 Nginx 反向代理 + HTTPS
├── 7.3 性能优化（缓存、并发）
├── 7.4 编写使用文档
├── 7.5 制作演示视频
└── 7.6 撰写简历话术

交付物：可部署的完整项目
```

---

### 8.2 关键决策点

**什么时候该做多智能体？**

| 条件 | 决策 |
|---|---|
| 单 Agent 能回答 80% 的问题 | 不需要多智能体 |
| 不同类型问题需要不同处理逻辑 | 需要路由 |
| 单 Agent 响应变慢或 prompt 变长 | 需要拆分 |

**实际情况**：已完成，Phase 2 实现了 LLM 意图分类 + 多 Agent 路由

**什么时候需要 LangGraph？**

| 条件 | 决策 |
|---|---|
| 需要复杂的状态流转 | 用 LangGraph |
| 需要人工介入暂停/恢复 | 用 LangGraph |
| 简单的线性流程 | 不需要 |

**实际情况**：Phase 2 用 if-else 路由跑通了基础流程。Phase 3（记忆）和 Phase 5（HITL）需要引入 LangGraph，因为：
1. LangGraph 的 `State` 天然支持 `messages` 列表，不需要自己管理对话历史
2. LangGraph 的 `interrupt` 机制是为人机协作设计的，HITL 直接用
3. 状态图比 if-else 可维护，后续加查询重写、情感检测等节点不用层层嵌套

---

### 8.3 时间规划

| 阶段 | 周期 | 状态 | 产出 |
|---|---|---|---|
| Phase 0 | 第 1-2 天 | ✅ 完成 | API 调通 demo |
| Phase 1 | 第 3-7 天 | ✅ 完成 | 单 Agent RAG + 增量同步 |
| Phase 2 | 第 8-14 天 | ✅ 完成 | 多 Agent 路由 + Prompt 优化 |
| Phase 3 | 第 15-21 天 | ✅ 完成 | LangGraph 集成 + 对话记忆 |
| Phase 4 | 第 22-28 天 |   待开始 | 查询重写优化 |
| Phase 5 | 第 29-35 天 |   下一步 | HITL 机制（LangGraph interrupt） |
| Phase 6 | 第 36-42 天 |   待开始 | 知识库后台 |
| Phase 7 | 第 43-49 天 |   待开始 | 部署上线 |

**总计**：约 7 周（50 天）

**当前进度**：Phase 0-3 已完成，下一步是 Phase 5（HITL 人工介入）

---

## 九、简历话术参考

### 项目名称
**智能客服系统** — 基于 LangGraph 多智能体架构的企业级实训设备服务平台

### 技术描述
- 设计 Supervisor 模式的多智能体协作架构，实现产品咨询、故障排查、培训资料三类任务的自动路由与分发
- 基于 RAG 架构实现知识库问答，采用 ChromaDB 向量数据库，支持语义检索与关键词混合搜索
- 实现查询重写模块，针对非专业用户的口语化提问，通过意图分类驱动的差异化改写策略，提升检索召回率
- 实现 HITL（Human-in-the-Loop）机制，对复杂/敏感问题自动触发人工审核流程，确保回答准确性
- 设计多轮对话记忆管理方案，支持会话级短期记忆与用户级长期记忆，实现上下文连续的智能交互
- 开发知识库管理后台，支持非技术人员通过 Web 界面动态更新知识库内容

### 技术栈
Python / LangChain v1.0 / LangGraph / FastAPI / ChromaDB / Gradio / DeepSeek API / 通义千问 Embedding (dashscope) / SQLite

---

## 十、待确认事项

| 序号 | 事项 | 负责人 | 状态 |
|---|---|---|---|
| 1 | 技术人员描述文档的格式（Word/Excel/其他） | 待确认 | |
| 2 | 图片和描述的对应关系（每张图一段描述/按章节整理） | 待确认 | |
| 3 | 图片文件名命名规则 | 待确认 | |
| 4 | 公司是否有 GPU 服务器 | 待确认 | |
| 5 | DeepSeek API Key 是否可用 | 待确认 | |
| 6 | 通义千问 Embedding API Key 是否可用 | 待确认 | |
| 7 | 公司内部通讯工具（企业微信/飞书/钉钉） | 待确认 | |
| 8 | 真实用户问答样本收集（50-100 条） | 进行中 | |

---

## 十一、风险与应对

| 风险 | 应对方案 |
|---|---|
| 公司没有 GPU 服务器 | 使用 DeepSeek API，无需 GPU；后续如需数据不出公司可迁移本地模型 |
| 产品资料格式混乱 | 用 unstructured 库处理，必要时人工整理 |
| 一个人进度慢 | Phase 1 快速出 demo 给领导看，争取资源 |
| 效果不够好 | 先覆盖高频问题，逐步优化 |
| 用户提问太口语化 | 查询重写模块 + 人工标注的高质量数据 |
